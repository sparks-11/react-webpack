const localStorageKey = {
  userDetails: 'userDetails',
  token: 'token',
  refreshToken: 'refreshToken',
};

const apiEndPoints = {
  test: 'test',
};

module.exports = {
  localStorageKey,
  apiEndPoints,
};
